{
  "name": "covert-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Covert — confidential compensation infrastructure on Zama FHEVM (smart contract layer)",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.ts",
    "seed": "hardhat run scripts/seed.ts --network sepolia",
    "export-abi": "hardhat run scripts/export-abi.ts",
    "verify": "hardhat verify --network sepolia",
    "lint": "echo 'no lint'"
  },
  "devDependencies": {
    "@fhevm/hardhat-plugin": "^0.4.2",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@fhevm/solidity": "^0.11.1",
    "@zama-fhe/relayer-sdk": "^0.4.1"
  }
}
